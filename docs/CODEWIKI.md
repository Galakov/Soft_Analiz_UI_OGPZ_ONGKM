# CodeWiki: Аналитика УИ ОГПЗ

**Версия**: 1.3.3
**Автор**: © Н.А. Галаков

---

## 📚 1. Обзор проекта

**Аналитика УИ ОГПЗ** — это инструмент для автоматизации обработки технологических отчетов. Программа объединяет данные из нескольких Excel-файлов, фильтрует их по параметрам и узлам измерения, и формирует итоговый отчет с умным форматированием (тепловая карта, границы узлов, тренды).

---

## 🚀 2. Быстрый старт (Установка)

### Системные требования
- **ОС**: RedOS (Linux) или Windows 10/11.
- **Python**: 3.6+.
- **GUI**: Требуется `python3-tkinter`.

### Установка на RedOS

1.  **Подготовка**:
    ```bash
    sudo dnf install python3 python3-pip python3-tkinter
    ```
2.  **Установка пакета**:
    Скопируйте файл `analytics_ui_ogpz-1.3.3-py3-none-any.whl` и выполните:
    ```bash
    pip3 install analytics_ui_ogpz-1.3.3-py3-none-any.whl
    ```
3.  **Настройка ярлыков**:
    ```bash
    analytics-ui-setup
    ```
4.  **Запуск**: Иконка на рабочем столе или команда `analytics-ui`.

---

## 📖 3. Руководство пользователя

### Интерфейс программы
- **Левая панель**: Загрузка файлов и выбор временного диапазона.
- **Центральная панель**: Список всех доступных параметров (температура, давление и т.д.).
- **Правая панель**: Список узлов измерения.
- **Нижняя часть**: Кнопка "Объединить файлы".

### Алгоритм работы
1.  Нажмите **"Добавить файлы"** и выберите исходные `.xlsx` таблицы.
2.  В центре выберите параметры, которые хотите видеть в отчете.
3.  Справа выберите нужные узлы измерения.
4.  (Опционально) Задайте временной интервал начала и конца.
5.  Нажмите **"Объединить файлы"** и сохраните результат.

### Особенности отчета
- **Объединенные заголовки**: Сверху отображается диапазон допустимых значений (например, `(10 ... 50 кгс/см2)`).
- **Авто-ширина**: Столбцы автоматически подстраиваются под содержимое.
- **Цветовая индикация**:
    - **Зеленый**: Высокие значения.
    - **Желтый**: Средние значения.
    - **Красный**: Низкие значения.
- **Стрелки трендов**: Если значение выходит за границы (Min/Max из правил), рядом появляется стрелка (`↑` или `↓`).

---

## ⚙️ 4. Конфигурация (Правила)

Программа управляется файлом `Правила названия столбцов.xlsx`.

| Столбец Excel | Назначение |
| :--- | :--- |
| **A** | Идентификатор файла (часть имени) |
| **B** | Исходное имя столбца в файле |
| **C** | Новое имя в отчете (Rename) |
| **D** | Узел измерения |
| **E** | Имя параметра (Ключевое слово для авто-единиц) |
| **F** | Min значение |
| **G** | Max значение |
| **H** | Единицы измерения (Опционально) |

> [!NOTE]
> **Автоматические единицы измерения**:
> Если столбец **H** пуст, программа пытается угадать единицы по столбцу **E** (Имя параметра):
> - "перепад давления" -> **кгс/см2**
> - "расход" -> **тыс. м3/ч**
> - "температура" -> **°C**

---

## 💻 5. Руководство разработчика

### Структура проекта
```text
Soft_Analiz_UI_OGPZ_ONGKM/
├── analytics_ui/               # Пакет приложения
│   ├── excel_merger.py         # Основной код (GUI + Логика)
│   ├── post_install.py         # Скрипт создания ярлыков Linux
│   └── Правила...xlsx          # Правила по умолчанию
├── docs/                       # Документация (CodeWiki, Manuals)
├── scripts/                    # Скрипты сборки (build_wheel.sh)
└── setup.py                    # Конфигурация пакета
```

### Сборка новой версии (.whl)
1.  Обновите версию в `setup.py`.
2.  Запустите сборку:
    ```bash
    # Windows (PowerShell)
    python -m build
    
    # Linux
    ./scripts/build_wheel.sh
    ```
3.  Файл появится в папке `dist/`.

### Основные модули (`excel_merger.py`)
- **`ExcelMerger`**: Класс GUI (Tkinter).
- **`merge_files`**: Логика слияния (Pandas).
- **`apply_conditional_formatting`**: Пост-обработка Excel (OpenPyXL) — цвета, границы, заголовки.
    - *Обновление v1.3.1*: Добавлен вывод единиц измерения в заголовки диапазонов.
- **`add_arrow_columns`**: Логика добавления стрелок трендов.

---

## 🐛 6. Решение проблем

**Ошибка `ModuleNotFoundError: No module named 'tkinter'`**
- Решение: `sudo dnf install python3-tkinter`

**Нет прав на запуск скриптов**
- Решение: `chmod +x script_name.sh`
