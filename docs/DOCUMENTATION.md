# Документация пользователя: Аналитика УИ ОГПЗ

**Аналитика УИ ОГПЗ** — это инструмент для автоматического объединения, анализа и форматирования отчетов в формате Excel. Программа разработана специально для работы в среде RedOS (и других Linux-системах), но также совместима с Windows.

---

## 📋 Содержание

1.  [Возможности программы](#возможности-программы)
2.  [Установка и настройка](#установка-и-настройка)
    *   [Системные требования](#системные-требования)
    *   [Установка пакета](#установка-пакета)
    *   [Создание ярлыков](#создание-ярлыков)
3.  [Руководство пользователя](#руководство-пользователя)
    *   [Интерфейс](#интерфейс)
    *   [Пошаговая инструкция](#пошаговая-инструкция)
4.  [Настройка правил](#настройка-правил)
5.  [Решение проблем](#решение-проблем)
6.  [Для разработчиков](#для-разработчиков)

---

## ✨ Возможности программы

*   **Объединение файлов**: Слияние нескольких Excel-файлов в один общий отчет.
*   **Фильтрация данных**: Выбор конкретных параметров и узлов измерения для включения в отчет.
*   **Умное форматирование**:
    *   Автоматическая цветовая раскраска ячеек (тепловая карта) от красного (низкие значения) до зеленого (высокие).
    *   Группировка столбцов границами по узлам измерения.
    *   Добавление стрелок (↑/↓) для значений, выходящих за пределы нормы.
*   **Временной фильтр**: Возможность обрезать данные по заданному временному интервалу.

---

## 🚀 Установка и настройка

### Системные требования

*   **ОС**: RedOS 7.3+ (или любой Linux с поддержкой RPM/DEB), Windows 10/11.
*   **Python**: Версия 3.6 или выше.
*   **Библиотеки**: `python3-tkinter` (для Linux).

### Установка пакета

1.  **Подготовка системы (только для RedOS/Linux)**:
    Убедитесь, что установлены необходимые системные компоненты:
    ```bash
    sudo dnf install python3 python3-pip python3-tkinter
    ```

2.  **Установка программы**:
    Скопируйте файл `analytics_ui_ogpz-1.2.4-py3-none-any.whl` на компьютер и выполните команду:
    ```bash
    pip3 install analytics_ui_ogpz-1.2.4-py3-none-any.whl
    ```

Главное окно программы разделено на три зоны:
1.  **Слева**: Управление файлами и временем.
2.  **Центр**: Выбор параметров (датчиков).
3.  **Справа**: Выбор узлов измерения.

### Пошаговая инструкция

1.  **Запуск**: Откройте программу через ярлык на рабочем столе или команду `analytics-ui`.
2.  **Добавление файлов**:
    *   Нажмите кнопку **"Добавить файлы"**.
    *   Выберите один или несколько файлов `.xlsx` или `.xls`.
    *   Программа автоматически проанализирует файлы и заполнит списки параметров и узлов.
3.  **Настройка фильтров**:
    *   **Параметры**: В центральном окне отметьте галочками параметры, которые нужно включить в отчет.
    *   **Узлы**: В правом окне выберите нужные узлы измерения.
    *   **Время**: (Опционально) Укажите начало и конец периода. Кнопка "Установить полный диапазон" выставит даты по загруженным файлам.
4.  **Объединение**:
    *   Нажмите кнопку **"Объединить файлы"**.
    *   Выберите место для сохранения итогового отчета.
    *   Дождитесь сообщения "Готово!".

---

## ⚙️ Настройка правил

Программа использует файл `Правила названия столбцов.xlsx` (встроен в пакет) для определения того, как обрабатывать данные.

**Структура файла правил:**
*   **Столбец A**: Часть имени файла (для идентификации).
*   **Столбец B**: Исходное название столбца в файле.
*   **Столбец C**: Новое название столбца в отчете.
*   **Столбец D**: Название узла измерения.
*   **Столбец E**: Название параметра.
*   **Столбец F**: Минимальное допустимое значение (для стрелок ↓).
*   **Столбец G**: Максимальное допустимое значение (для стрелок ↑).

---

## 🔧 Решение проблем

### Программа не запускается (ошибка `tkinter`)
Если при запуске вы видите ошибку `ModuleNotFoundError: No module named 'tkinter'`, значит не установлен графический интерфейс Python.
**Решение**:
```bash
sudo dnf install python3-tkinter
```

### Где найти логи ошибок?
Если программа работает некорректно, посмотрите файл логов:
*   **Linux**: `~/.analytics_ui/app.log`
*   **Windows**: `C:\Users\ИмяПользователя\.analytics_ui\app.log`

---

## 👨‍💻 Для разработчиков

### Структура проекта
*   `analytics_ui/` — Исходный код пакета.
    *   `excel_merger.py` — Основная логика и GUI.
    *   `post_install.py` — Скрипт создания ярлыков.
*   `scripts/` — Скрипты для сборки и установки.
*   `setup.py` — Конфигурация пакета.

### Сборка пакета (.whl)
Для создания новой версии пакета используйте скрипт:
```bash
./scripts/build_wheel.sh
```
Файл будет создан в папке `dist/`.
